spring:
  application:
    name: user-service

  cloud:
    config:
      uri: http://localhost:8888

  profiles:
    active: default

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
